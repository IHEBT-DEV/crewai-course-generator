services:
  crewai_api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crewai_api
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
    # REMOVED: command: uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --reload
